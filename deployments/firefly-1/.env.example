# deployments/firefly-1/.env
#
# Environment variables for the firefly-1 QRNG profile.
# Copy this file to .env and edit as needed:
#   cp .env.example .env

# --- Hugging Face ---
HF_MODEL=Qwen/Qwen2.5-1.5B-Instruct
# Set this if the model is gated.
HF_TOKEN=

# --- Entropy source (external QRNG) ---
QR_ENTROPY_SOURCE_TYPE=quantum_grpc
QR_GRPC_SERVER_ADDRESS=10.0.0.115:50051
QR_GRPC_MODE=unary
QR_GRPC_TIMEOUT_MS=5000
QR_GRPC_METHOD_PATH=/qrng.QuantumRNG/GetRandomBytes
# firefly-1 supports unary only — leave stream path empty.
QR_GRPC_STREAM_METHOD_PATH=

# --- Authentication ---
# Replace with your actual API key. Do NOT commit this file with real keys.
QR_GRPC_API_KEY=YOUR_API_KEY_HERE
QR_GRPC_API_KEY_HEADER=api-key

# Fall back to system entropy if the QRNG server is unreachable.
QR_FALLBACK_MODE=system

# --- Sampling parameters ---
# NOTE: 13312 bytes (13 KB) is the firefly-1 per-request limit.
# The default of 20480 would be rejected by the server.
QR_SAMPLE_COUNT=13312
QR_TEMPERATURE_STRATEGY=fixed
QR_FIXED_TEMPERATURE=0.7
QR_TOP_K=50
QR_TOP_P=0.9
QR_LOG_LEVEL=summary

# --- Ports (host-side) ---
VLLM_PORT=8000

# --- Open WebUI (optional, --profile ui) ---
OPEN_WEBUI_PORT=3000
# Set to true to require login (recommended for shared/public servers).
OPEN_WEBUI_AUTH=false
